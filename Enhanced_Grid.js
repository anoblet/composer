require([ "dojo/_base/declare", "dojox/grid/EnhancedGrid", ], function(declare,
		EnhancedGrid) {
	declare("Enhanced_Grid", [ EnhancedGrid ], {
		setStore: function(Store){
			var Structure = [];
			dojo.foreach(Store, function()
			{
				
			});
		}
	});
});
